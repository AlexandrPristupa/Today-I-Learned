import { arrayChange } from '../../javascript/codesignal/arrayChange';

describe('testing arrayChange', () => {

  it('should be return true', () => {
    expect(arrayChange([1,1,1])).toEqual(3);
  });

});
